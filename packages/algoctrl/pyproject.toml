[project]
name = "algoctrl-tmpl"
version = "0.1.0"
description = "Algorithmic ramp template with explicit control stages and observer hooks."
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["konch>=6.0.1", "ptpython>=3.0.32"]

[project.urls]
Repository = "https://github.com/<you>/algoctrl-tmpl"
